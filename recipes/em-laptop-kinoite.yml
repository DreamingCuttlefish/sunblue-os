name: "em-laptop"
description: The kinoite image of sunblue-os for EM's laptop

base-image: ghcr.io/ublue-os/kinoite-asus
image-version: latest 

modules:
  - type: files
    files:
      - usr: /usr 

  - type: rpm-ostree
    repos:
    install:
      - supergfxctl
    remove:

  - type: default-flatpaks
    notify: true 
    system:
      install:
      remove:
  - from-file: modules/mullvad-module.yml

  - type: signing 

